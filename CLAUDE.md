# חלוקת הרכבים — הנחיות עבודה

אפליקציה שמחלקת תלמידי כיתת אלתור להרכבים. משמשת בעיקר **בטלפון**, בכיתה,
תוך כדי שיעור. כל שינוי צריך להיבדק ב-390px לפני שהוא נחשב גמור.

## פרסום — לעשות תמיד אחרי שינויים

האפליקציה פרוסה ב-**Vercel**, שבונה מענף `main`. עבודה שנשארת על ענף פיתוח
לא מגיעה לאתר החי.

לכן, בסיום כל שינוי, בלי לחכות לבקשה:

```bash
npm run lint && npm test && npm run build   # חייב לעבור לפני מיזוג
git checkout main && git merge --ff-only <branch>
git push origin main
git checkout <branch>
```

**לבדוק את שלושתם לפי קוד היציאה, לא לפי טקסט.** `npx eslint . | tail -1`
מחזיר שורה ריקה גם כשיש שגיאה, ובגלל זה חמש דחיפות רצופות נכשלו ב-CI
בזמן שדווח "lint נקי". להריץ `npm run lint; echo $?` או בלי pipe בכלל.

**ואחרי הדחיפה — לוודא שה-CI עבר בפועל**, לא להסתמך על הריצה המקומית.

`.github/workflows/ci.yml` מריץ lint, בדיקות ובנייה על כל דחיפה ל-`main`.
הוא **לא** מפרסם — הפרסום הוא של Vercel בלבד.

## פקודות

- `npm test` — 82 בדיקות על `node:test`, בלי תלויות. חייב להיות ירוק.
  הפקודה היא `node --test` בלי תבנית glob: node 20 לא תומך בהן ו-CI נפל על זה.
- `npm run lint` — errors בלבד חוסמים; warnings של הוקים מקובלים.
  שים לב: `no-use-before-define` תופס גם קבוע שמוגדר בהמשך הקובץ ומשמש
  קודם — להגדיר קבועים בראש, ליד `MAX_LOAD`.
- `npm run dev` / `npm run build`

## מבנה

- `src/ensembles.js` — כל הלוגיקה, בלי React ובלי דפדפן. **כל לוגיקה חדשה
  שייכת לכאן**, כדי שתהיה ניתנת לבדיקה ישירה.
- `src/ensembles.test.js` — הבדיקות, כולל רגרסיות לבאגים שכבר תוקנו.
- `src/App.jsx` — תצוגה, נוכחות, מטמון מקומי.
- `src/sheets.js` — Google Sheets. `src/ErrorBoundary.jsx` — במקום מסך לבן.

## כללים שאסור לשבור

- **בכל הרכב תופים, בס, כלי הרמוני וכלי מלודי.** ארבעת אלה הם `REQUIRED`,
  והם התוכן של `missingRoles`. זה תקף גם אחרי שינוי נוכחות באמצע השיעור
  ואחרי עריכה ידנית. כשאי אפשר לשמור עליו — אומרים את זה במפורש ומוסיפים
  מה כן אפשרי, לא מעמידים פנים.
- **כלי משני הוא רזרבה בלבד.** האיבר החמישי ב-`RAW` הוא כלים משניים.
  `forRole` מחזיר את בעלי הכלי הראשי, ורק אם אין אף אחד כזה — את בעלי
  הכלי המשני. הרזרבה נכנסת בחיסור, לא כדי להקל על מי שנוכח: אם נשאר
  ולו בסיסט ראשי אחד, הוא זה שמנגן בכל ההרכבים. `enrichHarmony` תמיד
  לפי הכלי הראשי.
- **`MAX_LOAD` נסוג מפני תפקיד חיוני.** תלמיד לא ינגן ביותר משני הרכבים —
  אלא אם אין ברירה: בסיסט או מתופף יחיד בכיתה ינגן **בכל** ההרכבים, כי
  הדרישה ל-`REQUIRED` בכל הרכב גוברת. ההקלה תקפה רק לתפקידים החיוניים
  (`makeGroups` שלב 1 ו-`repairDraw`); `enrichHarmony` לעולם לא תחרוג.
  הסימון בממשק הוא ×N לפי העומס בפועל, לא ×2 קבוע.
- **`MAX_GROUP = 7` נאכף, לא מומלץ.** הוא נבדק בשלושה מקומות ורק בהם
  הרכב יכול לגדול: שלב המלודיה ב-`makeGroups`, צירוף המורה בסופה,
  ו-`enrichHarmony`. קודם הוא היה 6 ורק שיקול בניקוד — ובפועל יצאו
  הרכבים של שמונה נגנים ב-5% מהמקרים. אם מוסיפים מקום שמגדיל הרכב,
  הוא חייב לבדוק את התקרה.
- **`MIN_GROUP` נגזר מ-`REQUIRED`** — ארבעה תפקידים, ארבעה נגנים מינימום.
  אין לקבע אותו כמספר.
- **`MAX_GROUPS = 5`** — לעולם לא יותר מחמישה הרכבים בשיעור.
- **ההמלצה היא הכי מעט הרכבים שעומדים בתנאי.** `capacity` בוחרת את ה-k
  הקטן ביותר שבו אף אחד לא יושב בחוץ ואף הרכב לא עובר את `MAX_GROUP`.
  פחות הרכבים = יותר זמן במה לכל הרכב (רוטציה של לפחות פעמיים בשיעור)
  וגם פחות תלמידים שנדרשים לנגן פעמיים. הכלל הזה מחזיר 4 לי״א ו-3 לט׳
  בלי להכיר את הכיתה — הוא החליף את `PREFERRED_GROUPS`, `PREFER_SLACK`
  ו-`preferredK`, ואין להחזיר קבוע לכל כיתה.
  `options` מחזירה את הטבלה המלאה, והממשק מציג אותה בבורר — **המורה
  צריך לראות מה כל מספר אומר**, לא רק את המילה "מומלץ".
- **`emptyRoles` חוסם רק תפקיד בלי אף נגן.** מספר הנגנים בתפקיד אינו
  מגביל את מספר ההרכבים, כי נגן יחיד מכסה כמה שצריך.
- **הכפלה רק כשהספסל ריק.** `enrichHarmony` מצרף כלי הרמוני שני להרכב,
  ולשם כך מכפיל נגן שכבר מנגן. זה מותר רק כשאיש לא יושב בחוץ — אחרת זה
  קיפוח. היא רצה אחרי בחירת החלוקה ולא בתוך `makeGroups`.
- **שני כלים מלודיים זהים בהרכב הם דבר רצוי.** העונש בניקוד קל (`same * 12`)
  ולא חוסם — שני סקסופונים באותו הרכב הם מפגש מוזיקלי, לא תקלה.
  `UNIQUE` חל על תופים, בס, פסנתר וגיטרה בלבד.
- **תור המלודיה ממוין לפי הפנקס בסוף.** הקיבוץ לפי כלי מפזר כלים זהים,
  אבל `queue.sort` הגלובלי הוא מה שקובע מי יישב כשנגמרים הכיסאות. בלי
  המיון הזה, מקום בתור נקבע לפי גודל קבוצת הכלי במקום לפי הצבירה.
- **המזהה של תלמיד** (איבר רביעי ב-`RAW`) הוא המפתח בגיליון ובגיבויים.
  אין לשנות אותו לתלמיד קיים — גם לא כששמו משתנה.
  בשינוי שם תצוגה יש להוסיף שורה ל-`FORMER_NAMES`, אחרת גיליון שנכתב
  לפני עמודת המזהה מזהה לפי שם, וההיסטוריה של אותו תלמיד תאבד בשקט.
- **שינוי בשקט של נתוני הפנקס אסור.** אם משהו נזרק או לא נמצא — לדווח.

## הוגנות — החלטה, לא באג

**נגני ריתמיקה ינגנו יותר ממלודיים, וזה מקובל.** כל הרכב חייב תופים, בס
וכלי הרמוני, ואין די נגנים כאלה — אז הם נדרשים פעמיים. אין מה לתקן בזה,
ואין לנסות לאזן את זה בתוך השיעור.

**האיזון נעשה בין השיעורים, דרך הפנקס שנשמר לגיליון.** זה עובד, ונמדד:
כשמתופף צבר 30 והשניים האחרים 10, הוא קיבל 100 כיסאות ב-100 חלוקות
(פעם אחת לשיעור) והם 200 (פעמיים). הסבב בתוך כל תפקיד יוצא כמעט מושלם —
פער 0–2 על פני 20 שיעורים. יש על זה בדיקת רגרסיה.

אין לנסות לצמצם את הפער **בין** תפקידים על חשבון דברים אחרים. שער
"מעשירים רק את מי שצבר עד הממוצע" נבדק ונדחה: חסך 2 נקודות פער והפיל
את פסנתר+גיטרה מ-59% ל-20%.

## מה שכבר נשרף עלינו

- **TDZ:** משתנה נגזר שקורא ל-state שמוגדר אחריו נותן **מסך לבן**. קרה
  שלוש פעמים. eslint לא תופס את זה כשהקריאה בתוך callback (למשל `useMemo`).
  להגדיר את כל ה-state לפני הלוגיקה הנגזרת.
- **בנייה ובדיקות שעוברות אינן הוכחה שהאפליקציה עולה.** שני באגים שהפילו
  את האפליקציה לגמרי עברו את שניהם. להריץ בדפדפן.
- **בדיקה שעוברת מול הקוד הישן חסרת ערך.** בכל תיקון באג — לוודא שהבדיקה
  החדשה נכשלת מול ההתנהגות הקודמת.
- **אין לקבע מספר הרכבים בבדיקות.** לגזור אותו מ-`capacity(pool).rec` —
  עריכת רשימת התלמידים משנה את מה שאפשר, ובדיקה שמקבעת מספר נשברת בכל
  שינוי כזה. פעם אחת זה הפיל 13 בדיקות בבת אחת.
- **מספר ההרכבים אינו חופשי לבחירה בבדיקות.** `capacity(pool).rec` הוא
  המקור; הוא השתנה פעם אחת (מ-5 ל-4 בי״א) והפיל שתי בדיקות הוגנות שהניחו
  שהעומס בתפקיד יכול להשתנות. כשבתפקיד יש בדיוק `players * MAX_LOAD === k`
  כיסאות, העומס כפוי ואין לפנקס מה לאזן — זו התנהגות תקינה, לא באג.
- **`slot === "melody"` אינו מעיד על עומס 1.** גיטריסט שנכנס בשלב המלודיה
  מסומן melody, ויכול לנגן בשני הרכבים. בבדיקות שצריכות נגן יחיד — לבדוק
  `load[id] === 1` במפורש.
- **בדיקות שנשענות על חלוקה אקראית הן הפכפכות.** אין להניח שזוג תלמידים
  שרירותי ניתן להחלפה — לחפש זוג תקף, או לבנות חלוקה ביד. להריץ `npm test`
  כמה פעמים לפני שמכריזים שהוא ירוק.
